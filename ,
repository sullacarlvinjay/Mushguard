numbers = [1, 2, 3, 4, 5, 6]

result = list(
    map(lambda x: x ** 2,         # Transform: square numbers
        filter(lambda x: x % 2 == 0, numbers)  # Transform: keep only even
    )
)

print(result)  # [4, 16, 36]